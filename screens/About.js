import { View,Text } from "react-native";
import { createDrawerNavigator } from "@react-navigation/drawer";

const drawer=createDrawerNavigator();

export default function About(){
    return(
      <Text>Well come to About</Text>
    )
}
